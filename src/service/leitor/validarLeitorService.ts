import { Mensagens } from "../../base/mensagens";
import { LeitorDTO } from "../../dto/leitorDTO";

export class ValidarLeitorService extends Mensagens {
  constructor() {
    super()
  }

  validar(leitor: LeitorDTO) {
    
  }
}